## [0.2.0] - 2026-06-13

Wave B/C personal-agent release: memory depth, gateway resilience, cron automation, and prod guards.

### Highlights

- **Memory:** autoRag pilot, preTurn modes (`ADVICE`/`DO`/`DEBUG`/`SYNC`), semantic search (pgvector), secure notes, session ingest → episodic wing, Cursor IDE transcript mining
- **Gateway:** outbound queue + observability, HTML replies, unified slash registry, live-resume preamble fix, pairing rate-limit, run metrics in `/status`
- **Cron:** contextFrom pipeline, context artifacts, prod `cron.jobs` guard + morning alert, digest QA/length policy, wake-gate & script jobs, threat scan on skills
- **UX / ops:** `/undo`, `/find`, turn hooks, eval battery, docs split (`OPS.md` + `REFERENCE.md`), secret corruption protection

### Fixed

- Telegram long replies — rich fallback to 4096 multipart; outbox auto-downgrade on `message is too long`
- Outbox observability (I-50) and user ack (I-51)
- preTurn profile — excerpt only on first composed turn per session
- Doctor `morning-alert` — correct exit code for launchd
- Gateway live resume — skip skills/`onStart` reinjection when SDK resumes live
- Digest QA length, setup-home migrate guard, prod cron guard (I-38)

### Added

See [CHANGELOG.md](https://github.com/niksh06/irida/blob/v0.2.0/CHANGELOG.md#020---2026-06-13) for the full list (40+ items).

**Install:** Node ≥ 20, `npm install && npm run build`, `csagent auth login --stdin`. Quickstart in [README.md](https://github.com/niksh06/irida/blob/v0.2.0/README.md).

**Tests:** 445 passing (mocked SDK).
