HappyIn KB weekly sync into csagent-memory (issue I-15).

1) Run shell from CSAGENT_ROOT (use csagent-run.sh or npx tsx src/cli.ts):
   `memory import-md --kb-root "$CSAGENT_KB_ROOT" --domains kafka,python,security,docker,llm-agents`
   If CSAGENT_KB_ROOT unset, use `/Users/nsh/Downloads/agent_tutorial`.

2) Use CSAGENT_HOME and CSAGENT_DATABASE_URL from environment (production PG).

3) Report: imported count, commit hash from tool output, any errors.

4) Do not delete existing notes. Do not run destructive shell beyond import-md.
