{
  "name": "irida-wisp-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Wisp — the irida pet as a macOS menu-bar companion + floating overlay. Renders the TUI's glyph frames from dist/petTerminal.js and follows .agent/pet-state.json.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "chat": "electron . --chat",
    "demo": "electron . --demo"
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
